# HANDOFF — 2026-09-20, ~22:10 (сессия: работа + игра через MCP-мост)

## Итог сессии одной строкой
Мост работает, игра идёт живая (день 3), агент НЕ должен ломать себе голову пиксельными кликами: управлять игрой надо семантикой (`move-tile`, `move`, `nearby`), а `hero:select` НИКОГДА не нажимать — он открывает экран профиля, а не выделяет героя.

## Состояние партии (на момент хендоффа)
- Дата: неделя 2, день 3 `[3,2,1]`. Золото 5950 (+2000/день от города). Ресурсы: `[19,20,2,4,7,8,5950]`.
- **Брисса** (основной, Элементалист, специализация Водные/Ледяные Элементали): позиция (71,35), ход **1530**, армия — пикси + элементали (получены обменом). Идёт на опыт (сундуки ей = опыт).
- **Кальт** (сборщик, Воитель Стихии, Тактика+Обучаемость): позиция (67,42), ход **0** (день отработан, ресурсы собраны), у него 1 водный элементаль.
- Разведано ~(60-72, 26-44): 4 сундука (71,26 / 64,33 / 64,34 / 71,34), сад (62,35), фонтан (66,40), флаг (70,37 — взят, +300 хода), выходы ресурсов.

## ЧТО РАБОТАЕТ (проверено живьём сегодня)
1. **`move-tile` (x,y) — главный игровой инструмент.** Возвращает `"completed"` / `"Hero reached the commanded cell"`, герой сам проходит путь, стоимость списывается с хода. Именно так и надо играть: цель → `move-tile`.
2. **`move` (targetId)** — тоже работал (ресурс-цель → `completed`), но часто `uncertain`/`not_available` — зависит от маршрутного кэша.
3. **Обмен армией:** окно `exchange`, стрелки под слотами (id 430-436 слева, 440-444 справа, y≈153) — клик = мгновенная передача отряда соседу. Проверено: 15 пикси + 3 + 6 элементалей перешли Бриссе.
4. Диалоги: `message:accept`, `hero:close`, `exchange` OK-кнопка — цикл возврата на карту (`screen=adventure`) работает.
5. **Трюк с выделением:** после действий выделение героя теряется, `nearby` пустеет. Рабочий обход: сделать `map-click` по карте (он пере-выделяет героя) → `nearby` снова отдаёт цели → `move-tile`.
6. Город: стройка/найм/таверна (из ранних сессий, карта покрытия `docs/CAPABILITIES.md`).

## ЧТО СЛОМАНО / ДЫРЫ (приоритет починки)
1. **Выделение героя (задача 1).** `hero:select` (= оп 54) ОТКРЫВАЕТ ПРОФИЛЬ ГЕРОЯ, а не выделяет его — это главный источник «дрочилова» и открытий профиля. Нужен честный select: либо клик по клетке героя через структуры карты, либо клавиша H напрямую через `KeyAsync`.
2. **Переключение героев на карте.** `hero:switch` (оп 62) работает только на экране города (Space); на карте переключения нет. Второй герой ходит только руками/если сделать инструмент.
3. **Маршрутный кэш.** `nearby` часто отдаёт `not_available` (игра строит кэш от реального курсора). `move-tile` при этом работает — на него и опираться.
4. **Сундук: выбор «золото/опыт».** Не подтверждён адресно (левая/правая кнопка). Брисса сейчас тыкает сундуки, но выбор опыта не верифицирован.
5. **`build/autoplay.py`** — черновик автономного цикла. Написан, но: убрал `hero:select` (открывал профиль), исключил town/creatures из целей, добавил разведку. НЕ доведён: нет переключения героев, разведка глупая. Использовать как основу, не запускать вслепую.

## КАК ПРОДОЛЖИТЬ
```bash
cd C:/Users/user/Documents/Codex/2026-09-19/new-chat-2/outputs/hota-agent-bridge
python build/hotacli.py observe      # состояние
python build/hotacli.py nearby       # цели (после map-click!)
python build/hotacli.py move-tile 71 34
```
- Сервер: `http://127.0.0.1:18773/bridge/`, токен `C:/Users/user/AppData/Local/HotaMcp/connection.token`, перезапуск `build/restart-server.ps1`.
- Лог автоигры: `docs/knowledge/sessions/auto-play.log`.
- План: добить ход Бриссы (1530) → день 4 → чинить задачу 1 (честный select + переключение героев) → доводить autoplay → цель: победа в сценарии.

## Уроки этой сессии (уже в docs/LESSONS.md)
- Играть `move-tile`/`move`, НЕ пиксельными кликами. Пиксельный план залипает.
- `hero:select` = открытие профиля. Никогда.
- `move_to` по своему городу = открытие города, не движение.
- Сундуки: боевому герою — опыт, сборщику — золото.
- Перед `nearby` — `map-click`, иначе «Select a hero first».
